// Disable SSR for Tauri compatibility
/** Disable server-side rendering — required for Tauri desktop deployment. */
export const ssr = false;
/** Enable prerendering for static page generation. */
export const prerender = true;
